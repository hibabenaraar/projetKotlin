<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Fond coloré de la navbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/primary_blue"
        android:padding="16dp"
        android:gravity="center">  <!-- Ajout de gravity pour centrer les éléments horizontalement -->

        <!-- Nom de la marque -->
        <TextView
            android:id="@+id/tvBrandName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Inance"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="28sp"
            android:gravity="center"
        android:paddingBottom="16dp" />

        <!-- Barre de navigation avec icônes et texte -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:weightSum="3">

            <!-- Home -->
            <com.google.android.material.textview.MaterialTextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:drawableStart="@drawable/baseline_home_24"
                android:drawablePadding="8dp"
                android:text="Home"
                android:textSize="18sp"
                android:textColor="#FFFFFF"
                android:gravity="center"
                android:padding="8dp"
                android:textStyle="bold"/>

            <com.google.android.material.textview.MaterialTextView android:id="@+id/nav_services"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:drawableStart="@drawable/baseline_home_repair_service_24"
            android:drawablePadding="8dp"
            android:text="Services"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:padding="8dp"
            android:textStyle="bold"/>

            <!-- About -->
            <com.google.android.material.textview.MaterialTextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:drawableStart="@drawable/baseline_info_24"
                android:drawablePadding="8dp"
                android:text="About"
                android:textSize="18sp"
                android:textColor="#FFFFFF"
                android:gravity="center"
                android:padding="8dp"
                android:textStyle="bold"/>
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>
